// This service is deprecated as AI features have been replaced by QR code functionality.
// Keeping file placeholder to avoid build errors during transition.
export const generateCaption = async (imageBase64: string): Promise<string> => {
  return "Caption generation disabled";
};